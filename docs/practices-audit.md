# Common-base practices audit

- **Repo:** `loan-document-intelligence`
- **Catalog id:** `loan-document-intelligence` (package `loan_doc_intel`, env prefix `LOAN_DOC`)
- **Catalogue reference:** [`common-base-practices.md`](https://github.com/portable-genai/.github/blob/main/common-base-practices.md) (checks A1..G7)
- **Authoritative source:** reconciled to the maintainer's cross-repository audit matrix, authoritative on verdicts.
- **Note:** Every check was re-run against the current tree (greps executed, files opened), with the
  reference repo's package/env names (`cdd_sow_research`/`CDD`) substituted for this repo's
  (`loan_doc_intel`/`LOAN_DOC`). Verdicts reflect what is in the tree today, not intent.

Applicability: `loan-document-intelligence` ships a UI (`ui/`) and Terraform (`infra/terraform/`), so `[ui]` and
`[infra]` checks apply. It delegates identity to Cloud IAP and owns no login flow, so the
`[ui]` login-hardening check `C8` is N-A. **Load-bearing** checks (a FAIL breaks a shared
catalog guarantee) are A1-A6, C1-C5, D1-D3 and E1.

| Check | Verdict | Evidence / gap |
|---|---|---|
| **A1** Hexagonal core, stdlib-only domain `[all]` **(load-bearing)** | PASS | `grep -rE "google\|fastapi\|httpx\|pydantic\|boto3\|azure" src/loan_doc_intel/domain/` returns nothing. |
| **A2** Ports are `@runtime_checkable` Protocols, re-exported once `[all]` **(load-bearing)** | PASS | 13 Protocol classes across `ports/*.py`, all carry `@runtime_checkable`; all re-exported from `ports/__init__.py`. Grouped by concern (extraction/generation/governance/identity/observability/runtime/safety) rather than one file per port, but the contract still holds. |
| **A3** Swappable profiles by one config value `[all]` **(load-bearing)** | PASS | `LOAN_DOC_PROFILE = local\|gcp\|platform\|onprem`; per-port `adapters:` map in `config/settings.yaml`; the offline suite runs green with no `google-*` packages in the venv (`pip install -e ".[dev]"`). |
| **A4** One adapter constructor `Adapter(settings)` `[all]` **(load-bearing)** | PASS | `tests/contract/test_port_parity.py::test_adapter_constructs_with_single_settings_arg` parametrises over `SDK_FREE_PROFILES` (`onprem`,`local`) x every port; passes. |
| **A5** Lazy cloud imports in cloud adapters `[all]` **(load-bearing)** | PASS | `grep -n "^from google\|^import google" src/loan_doc_intel/adapters/gcp/*.py` returns nothing; the offline leg imports every module. |
| **A6** Contract tests enforce the hexagon; port map cannot drift `[all]` **(load-bearing)** | PASS | `test_port_parity.py` covers structural parity (`isinstance` + member presence), single-settings-arg and `runtime_checkable`, PLUS `test_port_protocols_matches_settings_adapters`: a set-equality guard that fails loudly on drift in BOTH directions (a `settings.adapters` binding with no `PORT_PROTOCOLS` entry, so untested; and a mapped port with no settings binding, so dangling). `tests/contract/test_behavioral_parity.py` proves same-request behavioral parity across implementations for three representative ports: redaction and guardrail assert `local == platform` at the domain boundary (the sibling guardrail gateway mocked with respx at its documented `/v1/redact` and `/v1/guardrail/screen` contract), audit asserts the stored (local) and POSTed (platform) record is byte-identical, and extraction (no `platform` sibling, direct-GCP only) pins local determinism across a re-run. Every port also pins the `onprem` fail-fast (`NotImplementedError`) contract, and an end-to-end test runs the full `LoanDocService.process` pipeline under `local` and fails fast under `onprem` on a profile change alone. |
| **A7** Kernel vs vertical split in the domain `[all]` | PASS | The seam is physical, not a label. `src/loan_doc_intel/domain/kernel.py` DEFINES the vertical-neutral machinery (`Citation`/`SourceType`, the LLM envelope `LlmMessage`/`LlmRequest`/`LlmResponse`/`ThinkingLevel`, `GuardrailCategory`/`Direction`/`GuardrailFinding`/`GuardrailVerdict`, `RedactionFinding`/`RedactionResult`, `Decision`/`AuditEvent`, `AgentSkill`/`AgentCard`/`ToolSpec`, `Severity`, `utcnow`, and the `StrEnum`/`TokenUsage`/`EvalReport`/`EvalMetricResult` commons re-exports) and imports NOTHING from this package. `src/loan_doc_intel/domain/models.py` keeps only the lending vertical (applicant, documents and extracts, income figures, cross-validation, the verification summary, the application case) and imports the kernel, re-exporting every kernel name with `from .kernel import (X as X,)` so no import site changed. Proved by EXECUTION in `tests/unit/test_kernel_boundary.py`: a fresh `subprocess` interpreter imports `loan_doc_intel.domain.kernel` and asserts `loan_doc_intel.domain.models` never enters `sys.modules`; the file also pins the reverse arrow, an AST scan finding no intra-package import in `kernel.py`, `is`-identity of all 23 kernel names across both modules, and the absence of all 15 vertical names from the kernel. Verified RED FIRST: with the previous shim restored from `HEAD` the same file reported 13 failed, 28 passed, including the executed subprocess assertion; after the split, 41 passed. |
| **A8** Consume platform horizontals via thin delegates `[all]` | PASS | `adapters/platform/remote_*.py` are 51-116 line marshalling-only HTTP clients (guardrail, redaction, audit, evaluation, registry); no second implementation of a horizontal sits outside a port. |
| **B1** Consequential math is deterministic, pure, replayable `[agentic]` | PASS | `domain/cross_validator.py` (345 lines, pure stdlib) is the verdict authority; `income_service.py` derives the summary deterministically; the LLM only normalises extracts into figures. Unit-tested in `test_cross_validator.py`, `test_income_and_hitl.py`. |
| **B2** Every claim carries a citation; empty grounding never ungrounded `[agentic]` | PASS | `Citation` on every claim-bearing type; `domain/_grounded.py::citations_for_document_ids` and `_build_figures` drop any document id the model returns that was not actually extracted, so no claim is emitted without a source document. (Grounding is extraction-based, so retrieve-then-cite's "empty retrieval hard error" is N/A; missing documents degrade to a `NEEDS_REVIEW` case, never an ungrounded answer.) |
| **B3** Maker-checker on every consequential output `[agentic]` | PASS | `requires_human_review` defaults `True` on the output models; `hitl.LoanReviewPolicy.requires_review()` always returns `True`; escalation only raises the bar (`is_escalated`). Asserted in `test_cross_validator.py`, `test_income_and_hitl.py`, `test_loan_doc_service.py`. |
| **B4** Bank-owned policy numbers in config, defaults = reference `[all]` | PASS | `ValidationSettings` owns amount, affordability and balance-decline thresholds; `deps.build_cross_validator()` injects them into the frozen pure-domain engine for API, service and CLI paths. Unit tests prove an override changes behaviour, invalid ordering refuses and replay is deterministic. |
| **B5** Open taxonomy: `StrEnum` vocabularies, engines typed on `str` `[all]` | PASS | All twelve vocabularies are `StrEnum` (via the shared `hex-service-kit` commons): members ARE their wire values and `.value` call sites are unchanged. |
| **C1** Identity resolved server-side; client actor/ACL discarded `[all]` **(load-bearing)** | PASS | `api/schemas.py` documents "no `actor` field"; `api/security.py::get_principal` resolves a verified `Principal` via the `IdentityPort` on every route (401 on failure); the audit actor is `principal.actor`. The IAP refusal is now split by whose failure it is, and the split is read at the API. In `adapters/gcp/iap_identity.py` the `LOAN_DOC_IAP_AUDIENCE` check already ran before the assertion header was read, so what changed here is the type it raises and the answer that follows: `IapAudienceUnconfiguredError`, not a plain `IdentityError`; the lazy google-auth import moved inside a `try` and raises `IapVerifierUnavailableError`. Both subclass `EndUserAuthUnavailableError` (declared in `ports/identity.py`, carrying `http_status`) and both answer 503, while an absent or blank assertion header stays a plain `IdentityError` and a bare 401 "authentication required" that tells a caller nothing they could forge with. `ctx.header(...)` is now `.strip()`ped, so a header a proxy or a deployment template rendered blank is refused as MISSING instead of reaching the algorithm pin. `api/security.py::get_principal` orders the `EndUserAuthUnavailableError` branch BEFORE the `IdentityError` branch, and the order is load-bearing: it is a subclass, so the reverse silently swallows it and answers the 401 the whole split exists to avoid. `tests/unit/test_iap_refusal_split.py` guards all of it. RED first, offline against the adapter exactly as it shipped: an unset audience gave `IdentityError: LOAN_DOC_IAP_AUDIENCE is not configured; cannot verify IAP assertion` whether or not the caller presented an assertion, and `api/security.py` answered 401 "authentication required" to it, sending an operator after a credential no credential would have supplied; a `"   "` header gave `IdentityError: assertion is not a compact JWS or JWE, so it carries no readable algorithm header`, a malformed token reported for an absent one; and with the `[gcp]` extra blocked, `ModuleNotFoundError: No module named 'google.auth.transport'` escaped `resolve` and `get_principal` for a bare 500 per request. The new module does not import against that tree at all; with only the two `Iap*Error` types injected and the old behaviour left standing it scored 8 failed / 3 passed, and reordering the two `except` branches on the fixed tree leaves 10 passed and fails `test_the_api_answers_a_deployment_failure_with_its_own_status_and_message` alone, 401 where 503 is owed. The fourth divergence from the `adapters/gcp/identity.py` family, the `principals` tuple, was investigated and deliberately NOT changed: this adapter grants `user:<subject>` and that family grants nothing, and it is this one that agrees with `hex_service_kit.federation.principal_from_iap_claims`, so aligning it would strip a verified identity of its own principal and widen nothing. The divergence is recorded in the test module rather than closed in the wrong direction. **The claim half is the commons decision too (2026-08-26).** `resolve()` no longer constructs a `Principal` at all: everything after the signature check is one `hex_service_kit.federation.principal_from_iap_claims(claims, _FEDERATION_POLICY, source="gcp-iap", include_subject_principal=True)` call, and the cryptography stays in the adapter because the kit's core is pure stdlib with zero runtime dependencies and verifies nothing. `_FEDERATION_POLICY` is a reviewed literal carrying `tenant_from_hosted_domain=True`: this deployment configures no domain map and IAP already restricts the audience to one organisation, so the `hd` claim IS the tenant id here, and that is stated as an OPT-IN rather than left to a fallback. `include_subject_principal` is stated for the same reason: this adapter family grants the verified subject its own `user:<subject>` principal while the `adapters/gcp/identity.py` family does not and the difference is an authorization decision, not a formatting one. `tests/unit/test_iap_claim_half.py` guards it, and it was RED first, offline, in three stages. (1) Against the adapter exactly as it shipped the module would not even import: `ImportError: cannot import name '_FEDERATION_POLICY'`, which is the state in which a load-bearing tenant can be emptied by a default nobody wrote down. (2) With only the policy object added and the hand-rolled tail left standing it scored 4 failed / 6 passed, and those four ARE the finding: the `acr` row and the mixed-case `hd` row of the comparison disagreed, and both `test_a_claim_set_from_another_issuer_cannot_become_a_principal` and `test_a_verified_assertion_that_names_nobody_is_refused` reported `DID NOT RAISE`, because with the claim-set precheck removed the hand-rolled tail read `claims["email"]` and built a verified identity out of a token issued by somebody else. (3) After adoption, 10 passed. Three deliberate defects on the fixed tree keep the guard honest: flipping `include_subject_principal` scores 6 failed / 4 passed, clearing `tenant_from_hosted_domain` fails the passthrough guard, and passing a fresh `FederationPolicy()` at the call site instead of the reviewed one scores 4 failed / 6 passed, on the resolved tenant rather than on an attribute. **Two behaviour changes are recorded rather than normalised away.** `assurance` now prefers an `acr` claim where the shipped tail hardcoded `"iap"`; nothing in this repository reads `Principal.assurance` for an authorization decision, so the change is audit-visible only, and IAP assertions do not carry `acr` in practice. The tenant is now lower-cased where the shipped tail passed `hd` through with only `.strip()`; `principal.tenant` is a partition key, so a mixed-case hosted domain would change the partition, and Google normalises `hd` to lower case in practice. Both were found by EXECUTING the commons against this adapter over the same claim sets, not by reading it. |
| **C2** Object-level authz derived server-side; tenant isolation by data tags `[all]` **(load-bearing)** | PASS | `ports/entitlements.py` (`EntitlementsPort`) resolves an `ObjectOwner` (owning tenant + permitted roles) from a SERVER-SIDE store keyed by object id, never from the request body; `domain/entitlements.py::require_object_access` is fail-closed (unknown object, empty tenant, wrong tenant and right-tenant-wrong-role all deny) and runs as step 0 of `LoanDocService.process` / `extract_only`, before any redaction, extraction or audit. Proven by `test_entitlements.py::test_cross_tenant_principal_is_denied` / `test_unknown_object_is_denied` / `test_local_adapter_end_to_end_cross_tenant_denied` and, at the route layer, `test_api_identity.py::test_cross_tenant_persona_is_denied_403_with_no_side_effects` (403 with no side effects). Each was RED before the fix. |
| **C3** Redact before everything `[agentic]` **(load-bearing)** | PASS | Redaction is step 1 of `LoanDocService._process_inner` (before guardrail, model, audit); extracted free-text fields are re-redacted in `_redact_extract`; `AuditEvent` stores only `redacted_prompt`/`redacted_response`. |
| **C4** Jurisdiction-driven PII packs keep the gate honest `[agentic]` **(load-bearing)** | PASS | `domain/pii_patterns.py` is the single SG/HK/JP/AU pack (rows ported verbatim from `onprem-dlp`, not narrowed), selected by `pii.jurisdictions` / `LOAN_DOC_PII_JURISDICTIONS`, and it drives all three consumers: the local redactor, the DLP adapter's custom info types, and the eval leak check. Row ORDER is this vertical's own: the `BANK_ACCOUNT_NUMBER` row (the 3-6-1 shape a statement prints) runs FIRST, because the AU TFN row matches the leading nine digits of an account and some pass the TFN checksum (`123-456782-0`, `123-000007-0`), which would report an applicant's account as a tax file number; pinned both ways in `tests/unit/test_redaction_service.py`. Not-falsely-green is proven PER MARKET by execution: with redaction disabled all four PII cases go red, and with one market's row broken while it is still configured ONLY that market's case goes red. RE2-safe forms are kept for DLP (which rejects the JP row's lookarounds outright) and pinned by `test_every_dlp_pattern_is_re2_compatible`. |
| **C5** Fail-closed defaults everywhere `[all]` **(load-bearing)** | PASS | `main()` binds via `hex_service_kit.resolve_bind_host` (loopback under the no-auth local profile unless `LOAN_DOC_ALLOW_INSECURE_DEMO=1`); Makefile `API_HOST ?= 127.0.0.1`; CORS is `cors_allowlist` (explicit `LOAN_DOC_CORS_ORIGINS`, never `*`; dev-origin fallback ONLY under local). Proven by `tests/unit/test_netdefaults.py`. |
| **C6** Security-header baseline on every surface `[ui]` | PASS | The Next console has one nonce-based CSP source plus a built-server hydration assertion. The API emits the three-state `frame-ancestors` policy, matching legacy frame option where expressible, `nosniff`, `no-referrer`, and HSTS only for managed profiles; the local probe asserts the non-TLS posture does not claim HSTS. |
| **C7** S2S calls authenticated, https-only outside loopback `[all]` | PASS | `adapters/platform/_s2s.py` sources `hex_service_kit.s2s`; all six platform delegates (`remote_audit`, `remote_guardrail`, `remote_entitlements`, `remote_redaction`, `remote_registry`, `remote_evaluation`) validate their base URL at construction and attach the S2S bearer + optional signed actor (headers `X-Ld-Actor`/`-Sig`). |
| **C8** Web login flow hardening `[ui]` | N-A | The repo owns no login: identity is delegated to Cloud IAP (`gcp`/`platform`), seeded dev personas (`local`), or an on-prem IdP placeholder (`onprem`). No OIDC adapter, no `api/auth.py`, no PKCE/JWKS/nonce. |
| **C9** Tamper-evident audit with honest limits `[all]` | PASS | `LocalAppendOnlyAuditAdapter` wraps the shared `hex_service_kit.audit.HashChainedAuditLog`: SHA-256 chain, UPDATE/DELETE triggers, JSONL export/restore, `verify_chain()`, honest-limits docstring. Proven by `tests/unit/test_audit_chain.py`. |
| **C10** No secret values in the repo `[all]` | PASS | `config/settings.yaml` stores only `*_env` names and KMS resource paths; literal-secret grep over `config/` is clean. |
| **D1** Locked, reproducible installs everywhere `[all]` **(load-bearing)** | PASS | `requirements-dev.lock` + `requirements-gcp.lock` are committed (uv pip compile); `ruff==0.15.18` is exact-pinned in `pyproject.toml`; the Dockerfile installs `-r requirements-gcp.lock` then `pip install --no-deps .`, and CI installs from the lockfile. |
| **D2** Digest-pinned images, SHA-pinned Actions, dependabot, CI audit `[all]` **(load-bearing)** | PASS | Both Dockerfile stages pin `python:3.12-slim@sha256:423ed6ab...`; every Action is 40-char SHA-pinned with a `# v4`/`# v5` comment; `.github/dependabot.yml` is present; `ci.yaml` has a `supply-chain` job running `pip-audit` over both lockfiles as a hard gate (it reports no known vulnerabilities). `npm audit --audit-level=high` is advisory pending the Next.js major bump, which is an upstream issue rather than a repo one. |
| **D3** Whole gate runs offline, zero org secrets `[all]` **(load-bearing)** | PASS | `ci.yaml` + `eval-gate.yaml` set `LOAN_DOC_PROFILE: local`, reference no `secrets.*`, and run ruff check + ruff format --check + mypy + pytest (integration deselected) + e2e smoke + `python eval/run_eval.py`. |
| **D4** Non-root, minimal, healthchecked container `[infra]` | PASS | Multi-stage `Dockerfile`: `USER appuser` (uid 10001), `HEALTHCHECK` on `/healthz`, `EXPOSE 8092`, `LOAN_DOC_PROFILE=gcp` in the image; the runtime stage copies only the venv (no build toolchain). |
| **D5** Deploy-time residency/sovereignty, parameterised `[infra]` | PASS | Terraform pins and validates Singapore, digest-pins the API image, binds CMEK to Cloud Run and the audit bucket, applies resource-location/no-SA-key Org Policy, starts VPC-SC in explicit dry-run, alerts on guardrail/key/perimeter/CMEK signals and runs fmt/validate in CI. `production_mode` rejects a partial posture; disposable demos keep irreversible lock and deletion protection explicit. Live enforcement is still unproved and externally blocked: it needs a `terraform apply` against a NAMED GCP project, plus the resulting evidence (the Org Policy constraints in force, the CMEK key bindings, and the VPC-SC perimeter moved out of dry-run). No code change can supply that. |
| **E1** Offline eval smoke guards merge; `model-quality-gate` owns promotion `[agentic]` **(load-bearing)** | PASS | `eval/run_eval.py` has the `--mode smoke|gate` split via the shared `agent-eval-kit` scaffold; `remote_evaluation.py` re-based on the shared `PromotionGateClient` (registered bundle `doc5-loan-document-intelligence` unchanged); gate mode refuses to run outside `LOAN_DOC_PROFILE=platform|gcp`. The two-part pii-kit eval scorer is untouched. |
| **E2** Safety metric with strictest threshold, no false green `[agentic]` | PASS | `pii_safety >= 0.99` runs the REAL `LocalRegexRedactionAdapter`, not a `FakeRedactionAdapter` with its own copy of the three regexes, so the gate cannot drift from the shipped redactor, and is scored TWO ways: off the shared pack (catches PII the pipeline re-introduced) and off each case's own planted identifier, pack-independently (catches the pack itself being wrong, which the pack scan is blind to by construction, since a row that fails to match cannot detect what it failed to mask). Verified by execution: break one market's row with the market still configured and the pack scan alone reports NO leak while the raw identifier sits verbatim in the audit record; only the literal check fires. A market missing from the config raises rather than scoring a vacuous 1.0. Four per-market golden cases (`case-pii-sg\|hk\|jp\|au`) carry their own jurisdiction's identifier, so a broken row reddens exactly one case. Both redaction call sites are covered: the audited prompt and the extracts the model reads. |
| **E3** Fixtures and golden data obviously fictional `[all]` | PASS | `eval/datasets/golden_cases.jsonl` uses "Casey Fictional", "1 Test Lane, Singapore 000001", `gs://fictional/...` and states "All applicant data is fictional"; DEMO.md drives synthetic applications. |
| **F1** Demo is code, offline, one command, presenter-paced `[all]` | PASS | `make demo-ui` starts the real local service, opens the headed browser, supports list/resume/screenshots and cleans up both processes. `scripts/run_ui_demo.py` owns lifecycle and forwards walkthrough options; `make demo` remains the separate static audit-view render. |
| **F2** Demo cannot rot silently `[all]` | PASS | `scripts/demo_selftest.py` exercises the rendered opening, verified, inconsistent, evidence and review states in CI; `data-demo` hooks are stable; unit tests pin step selection, pause order, no-input unattended mode, cleanup and narration hygiene. |
| **F3** Portability claim is executable `[all]` | PASS | `scripts/portability_demo.py` gates complete profile maps, platform seams, deterministic local replay, hash-chained audit, managed-adapter construction and fail-fast on-premises boundaries. It explicitly states that live GCP, model-quality parity, complete on-prem adapters, cross-issuer identity and managed WORM enforcement are not proved. |
| **G1** Declared doc authority order, kept true `[all]` | PASS | README declares `SPEC > ARCHITECTURE > COMPLIANCE > README`; no stale shipped-feature contradiction was found. |
| **G2** Compliance mapping table + adopter-owned crosswalk `[all]` | PASS | COMPLIANCE now includes an explicitly adopter-owned responsible-lending/MAS crosswalk with applicability, owner and evidence fields. |
| **G3** Documented, mechanised fork path `[all]` | PASS | `docs/ADOPTING.md` documents the explicit `domain/kernel.py` boundary, the core-vs-adopter-owned file list, the one-pass rebrand and the human-decision checklist; `scripts/rename_fork.py` mechanises the rename (package `loan_doc_intel`, CLI/dist/resource stem `loan-document-intelligence`, env prefix `LOAN_DOC_`, `--dist` defaulting to `--resource`). Dry-run verified: exit 0, plan of 64 files / 344 replacements, writes nothing (`git` stays clean). |
| **G4** Retired `[all]` | N-A (retired) | Retired practice. Releases are tracked by git tag and the `pyproject.toml` version. |
| **G5** Role-specific FAQs referencing sibling systems `[all]` | PASS | `docs/faq/` ships a README index plus five role FAQs (security, portability, features, adoption, compliance); each names the owning catalog id for the adjacent concern (`agent-guardrail-gateway`, `model-quality-gate`, `agent-observability`, `human-review-console`, the cloud control-mapping toolkit/`architecture-validator`/the data-residency validator/the exit-and-portability planner/`onprem-dlp`) and marks the `enterprise-knowledge-base` governed-RAG dependency N/A, pointing at the boundary rather than duplicating it. |
| **G6** Contribution docs cover full extension touch list `[all]` | PASS | CONTRIBUTING lists adapter and sub-service touch points and names the enforcing parity test. |
| **G7** Markdown discipline: minimise em-dashes, validate mermaid `[all]` | PASS (minor drift) | Core docs (README, SPEC, ARCHITECTURE, COMPLIANCE, CONTRIBUTING) and `docs/*.md` are at 0 em-dashes (the repo uses " : " spaced colons); only DEMO.md carries 4. Convention is "minimise", non-load-bearing. |

**Verdict counts: 39 PASS, 0 PARTIAL, 0 FAIL, 2 N-A.** The one
remaining external prerequisite is **D5**: live residency/sovereignty enforcement needs a
named GCP `terraform apply` against a real project, with the resulting Org Policy, CMEK
binding and VPC-SC perimeter evidence captured. That is external evidence, not a code
verdict, and it does not reopen a row.
Load-bearing (A1-A6, C1-C5, D1-D3, E1): all 15 PASS, **0 PARTIAL, 0 FAIL**.

**Every load-bearing check PASSes.**

## Gaps carried to systems/

The `loan-document-intelligence` row of the maintainer's per-system register
records the gaps below. The load-bearing ones (they break a shared catalog guarantee) are
marked.

- **Named deployment evidence (outside the practice verdicts).** Terraform validation is green;
  a reviewed GCP plan/apply/rollback and target-host browser/audit record still require the
  installation inputs listed in `DEMO.md` and `infra/terraform/README.md`.
